import { ComingSoon } from '@/components/coming-soon';

// The Plan tab (route name "index" so the app opens here). The weekly plan
// is the next screen to design after the shopping list.
export default function PlanScreen() {
  return <ComingSoon title="Plan" />;
}
